@model List<NissGram.Models.Post>

<div class="row row-cols-1 row-cols-md-3 g-4 compressed-row">
    @foreach (var post in Model)
    {
        <div class="col">
            <partial name="_PostNote" model="post" />
        </div>
    }
</div>
