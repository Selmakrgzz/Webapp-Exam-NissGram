import React from 'react';
/*import Dropdown from '../profile/Dropdown'; // Import the Dropdown component

interface CustomDropdownProps {
  icon: JSX.Element;
  menuItems: Array<{ label: string; action: () => void }>;
}

const CustomDropdown: React.FC<CustomDropdownProps> = ({ icon, menuItems }) => {
  return (
    <div className="dropdown-container">
      <button
        className="btn btn-link"
        type="button"
        id="dropdownMenuButton"
        data-bs-toggle="dropdown"
        aria-expanded="false"
      >
        {icon}
      </button>
      <ul className="dropdown-menu dropdown-menu-end" aria-labelledby="dropdownMenuButton">
        {menuItems.map((item, index) => (
          <li key={index}>
            <button type="button" className="dropdown-item" onClick={item.action}>
              {item.label}
            </button>
          </li>
        ))}
      </ul>
    </div>
  );
};

export default CustomDropdown;
*/